# Planora Backend API

Backend API services for Planora - Event Management Platform. Built using Node.js, Express, Prisma ORM, and PostgreSQL.

## Getting Started

1. Set up your `.env` file configuration.
2. Run `npm install` to download dependencies.
3. Run `npm run dev` to start the development server.
